export type CardType = {
   id: number
   title: string
   img: string
   price: number
   like: boolean
}

export type ShortCardType = {
   id: number
   title: string
   img: string
   price: number
}